const BigMinus = ({ className }) => {
    return (
        <div className={`w-12 h-12 bg-transparent flex items center justify-center ${className}`}>
            <div className="w-full h-2 bg-white"></div>
        </div>

    );
}
export default BigMinus;